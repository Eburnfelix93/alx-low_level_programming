#include <stdio>
/**
 * main - Write a program that prints the alphabet in lowercase
 *
 * Return: 0.
 */
int main(void)
{
	char alpha;

	for (alpha = 'a'; alpha <= 'z'; alpa++)
		putchar(alpha);

	putchar('\n');

	return (0);
}
